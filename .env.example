# Your wallet Private key
# IMPORTANT: Keep this secret and never commit it to version control
PRIVATE_KEY=0x...

# Path to your endpoints configuration file
# Default: ~/.x402-agent/endpoints.json
X402_CONFIG_PATH=~/.x402-agent/endpoints.json

# Enable debug logging (set to 'true' or '1')
# Default: false
DEBUG=false
